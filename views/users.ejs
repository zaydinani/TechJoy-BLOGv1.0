<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>TechJoy ||Admin</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/normalize/5.0.0/normalize.min.css"
    />
    <link rel="stylesheet" href="./css/dashboard-style.css" />
    <link rel="icon" type="image/x-icon" href="./images/icons8-read-96.png" />
  </head>
  <body>
    <!-- partial:index.partial.html -->
    <div class="app-container">
      <!--!   -----------------------------------  side bar -------------------------------------------------------------------->
      <%- include('./sidebar'); -%>

            <!--!   -----------------------------------  user tab -------------------------------------------------------------------->
            <div class="users tabcontent slide-in-top" id="users">
                <div class="app-content-header">
                  <h1 class="app-content-headerText">users</h1>
                  <button class="app-content-headerButton">Add user</button>
                </div>
                <div class="app-content-actions">
                  <input class="search-bar" placeholder="Search..." type="text" />
                </div>
                <div class="products-area-wrapper tableView">
                  <div class="products-header">
                    <div class="product-cell image">full name</div>
                    <div class="product-cell admin">email</div>
                    <div class="product-cell date">date created</div>
                    <div class="product-cell">delete</div>
                  </div>
                  <% for(let i = 0; i < users.length ; i++) { %>
                  <div class="products-row">
                    <div class="product-cell image">
                      <img
                        src=<%= users[i].image_pathLocation  %>
                        alt="product"
                      />
                      <span><%=users[i].user_name %></span>
                    </div>
                    <div class="product-cell admin">
                      <span class="cell-label">email:</span><%=users[i].user_email %>
                    </div>
                    <div class="product-cell date">
                      <span class="cell-label">date-created:</span><%=users[i].user_created_at.toLocaleDateString('en-US', {
                        month: 'long',
                        day: 'numeric',
                        year: 'numeric'
                      })  %>
                    </div>
                    <div class="product-cell">
                      <a  class="delete" href="/admin/delete/user/<%=users[i].user_id%>">delete</a>
                    </div>
                  </div>
                  <% } %>
                </div>
              </div>
              
    <!-- partial -->
    <script src="./JavaScript/dash-JS.js"></script>
</body>
</html>